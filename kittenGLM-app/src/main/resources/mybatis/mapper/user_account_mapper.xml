<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitten.infrastructure.dao.IUserAccountDao">
    <resultMap id="userAccountMap" type="com.kitten.infrastructure.po.UserAccountPO">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="total_quota" property="totalQuota"/>
        <result column="surplus_quota" property="surplusQuota"/>
        <result column="model_types" property="modelTypes"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryUserAccount" parameterType="string" resultMap="userAccountMap">
        select openid, total_quota, surplus_quota, model_types, status, create_time, update_time
        from openai.user_account
        where openid = #{openid}
    </select>

    <update id="subAccountQuota" parameterType="java.lang.String">
        UPDATE openai.user_account SET surplus_quota = surplus_quota - 1
        WHERE openid = #{openid} AND surplus_quota > 0 AND status = 0
    </update>

<!--  订单发货, 新增账户用量  -->
    <update id="addAccountQuota" parameterType="com.kitten.infrastructure.po.UserAccountPO">
        update user_account set
        total_quota = total_quota + #{surplusQuota},
        surplus_quota = surplus_quota + #{surplusQuota},
        update_time = now()
        where openid = #{openid}
    </update>
<!-- 新增用户账户 -->
    <insert id="insert" parameterType="com.kitten.infrastructure.po.UserAccountPO">
        insert into user_account (openid, total_quota, surplus_quota, model_types, status, create_time, update_time)
        values (#{openid}, #{totalQuota}, #{surplusQuota}, 'glm_4_flash' ,0, now(), now())
    </insert>


</mapper>